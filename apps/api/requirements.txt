fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.13.5
pydantic-settings==2.15.0
sqlalchemy==2.0.35
geoalchemy2==0.15.2
psycopg2-binary==2.9.12
# Direct dependency: services/routing/graph.py uses it to snap
# coordinates to the nearest junction. It also arrives transitively via
# geopandas/scikit-learn, but a direct import belongs in this file.
numpy==2.5.3
geopandas==1.0.1
shapely==2.1.2
# Terrain sampling reads Copernicus DEM cloud-optimised GeoTIFFs over
# HTTP range requests, so no 288 MB of tiles has to be downloaded.
# See geo/dem/sample_road_elevation.py.
rasterio==1.5.1
networkx==3.3
ortools==9.15.6755
scikit-learn==1.7.2
# Direct dependency: logistics routing runs scipy's compiled Dijkstra over a
# sparse cost matrix (services/logistics/routes.py). Also a scikit-learn
# dependency, but a direct import belongs here, pinned to a version with a
# Linux cp312 wheel.
scipy==1.17.1
requests==2.32.3
# Hazard ingestion parses the DRIMS daily report, which is published
# as a PDF -- there is no structured feed. See services/ingestion/.
pdfplumber==0.11.4
pytest==8.3.3
httpx==0.27.2
